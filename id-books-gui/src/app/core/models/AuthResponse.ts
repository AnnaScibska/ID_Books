export class AuthResponse {
  public token?: string;
  public avatar?: string;

  // fromJSON(json) {
  //   json.forEach(propName => {
  //     this[propName] = json[propName];
  //     return this;
  //   });
  // }
}
